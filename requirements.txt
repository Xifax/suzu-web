# NB: should not be regenerated using pip freeze > requirements.txt
# as it will remove all the comments.
# Instead, use make update (not implemented yet)

# Web microframework
bottle==0.10.11
# Web requests and debugging tools
Werkzeug==0.8.3
# Werkzeug plugin for bottle
bottle-werkzeug==0.1
# Session management
Beaker==1.6.4
# MongoDB ORM
mongoengine==0.6.20
# Templater
Jinja2==2.6
# Static assets plugin for Jinja
webassets==0.7.1
# Additional tags for Jinja [unused]
#jinja2modern==0.2.0
# Unit testing utility
nose==1.2.1
# Test coverage plugin for nose
coverage==3.5.3
# Mocks for tests
mock==0.8.0
# Dependency injection for tests
snake-guice==0.4
# Documentation tool
sphinx==1.1.3
# HTTP library
requests==0.14.0
# HTML parser wrapper
BeautifulSoup4==4.1.3
# HTML parser
lxml==2.3.6
# Convert kana
jcconv==0.1.2
# Crawl thy web
Scrapy==0.16.3
# Cli tool
argh==0.23.0

